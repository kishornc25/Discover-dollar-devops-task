version: '3.8'

services:
  backend:
    build: ./backend
    container_name: backend_container
    ports:
      - "3000:3000"
    command: ["node", "server.js"]

  frontend:
    build: ./frontend
    container_name: frontend_container
    ports:
      - "8080:80"
    depends_on:
      - backend
